{
    "title": "{{translated.settings}}",
    "description": "{{translated.gamesettings}}",
    "type": "object",
    "properties": {
      "difficulty": {
        "type": "string",
        "required": true,
        "default": "easy",
        "enum": ["easy", "medium", "hard"]
      },
      "tileset": {
        "type": "string",
        "required": true,
        "default": "sports",
        "enum": ["sports", "animals"]
      }
    }
}